# Updated Environment Configuration

# Browser-safe (keep existing)
VITE_SUPABASE_URL=https:"URL"
VITE_SUPABASE_ANON_KEY="key"

# Server-only (add these)
SUPABASE_DB_URL=postgresql:URL
HUGGINGFACE_API_KEY= key
